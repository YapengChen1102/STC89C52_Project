#ifndef __Nixie_Disp_H__
#define __Nixie_Disp_H__

void delay(unsigned int);
void ShowNum(unsigned int column, unsigned int number, unsigned int length);
void ShowChar(unsigned int column, unsigned char Char);

#endif